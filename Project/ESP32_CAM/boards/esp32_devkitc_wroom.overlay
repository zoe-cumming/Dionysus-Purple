/ {
   leds {
    compatible="gpio-leds";
    led_flash: led_flash {
        gpios = <&gpio0 4 GPIO_ACTIVE_HIGH>;
        label = "Flash LED";
        };
    };

    aliases {
        led0 = &led_flash;
    };
};